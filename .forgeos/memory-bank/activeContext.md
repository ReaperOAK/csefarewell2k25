---
id: active-context
version: "1.0"
owner: Shared
write_access: [ALL]
append_only: true
---

# Active Context

> Append-only. Each agent writes a `### [{ticket-id}] — Summary` entry here
> before marking a ticket DONE (the memory gate). Newest entries at the bottom.

<!-- ForgeOS will append ticket summaries below this line. -->
